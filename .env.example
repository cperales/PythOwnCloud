# PythOwnCloud Server configuration
# Copy this to .env and change the values

# Secret API key — generate one with: python3 -c "import secrets; print(secrets.token_urlsafe(32))"
POC_API_KEY=a1b2c3d4

# Storage path inside the container (usually no need to change)
POC_STORAGE_PATH=/data
